<Window x:Class="FreeFlow.Wpf.Views.DestinationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Destination"
        Width="520" Height="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Upload destination"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,12"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Name" VerticalAlignment="Center" Margin="0,0,8,10"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Protocol" VerticalAlignment="Center" Margin="0,0,8,10"/>
            <ComboBox x:Name="ProtocolCombo"
                      Grid.Row="1" Grid.Column="1"
                      SelectedItem="{Binding Protocol}"
                      Margin="0,0,0,10"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Host" VerticalAlignment="Center" Margin="0,0,8,10"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Port" VerticalAlignment="Center" Margin="0,0,8,10"/>
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Username" VerticalAlignment="Center" Margin="0,0,8,10"/>
            <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <TextBlock Grid.Row="5" Grid.Column="0" Text="Password" VerticalAlignment="Center" Margin="0,0,8,10"/>
            <PasswordBox x:Name="PasswordBox"
                         Grid.Row="5" Grid.Column="1"
                         Margin="0,0,0,10"/>

            <TextBlock Grid.Row="6" Grid.Column="0" Text="Remote path" VerticalAlignment="Center" Margin="0,0,8,10"/>
            <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding RemotePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

            <CheckBox Grid.Row="7" Grid.Column="1"
                      Content="Enabled"
                      IsChecked="{Binding IsEnabled}"
                      Margin="0,8,0,0"/>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Width="100" Margin="0,0,8,0" IsCancel="True"/>
            <Button Content="Save" Width="100" IsDefault="True" Click="OnSaveClicked"/>
        </StackPanel>
    </Grid>
</Window>

